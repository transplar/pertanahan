language: node

before_script: yarn install

script: yarn build

deploy:
  provider: pages
  skip_cleanup: true
  local_dir: build
  github_token: $GITHUB_TOKEN
  keep_history: false
  on:
    branch: master
